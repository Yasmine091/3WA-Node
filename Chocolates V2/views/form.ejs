<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= chocolate ? "Edit chocolate" : "Add new chocolate" %></title>
</head>
<body>
    <h1><%= chocolate ? "Edit chocolate" : "Add new chocolate" %></h1>
    <form action="<%= chocolate ? `/api/chocolates/${chocolate._id}?_method=PUT` : '/api/chocolates' %>" method="POST">
        <label for="name">Name:</label><br>
        <input id="name" type="text" value="<%= chocolate ? chocolate.name : '' %>" name="name" required><br>
        <br>
        <label for="description">Description:</label><br>
        <input id="description" type="text" value="<%= chocolate ? chocolate.description : '' %>" name="description" required><br>
        <br>
        <label for="brand">Brand:</label><br>
        <input id="brand" type="text" value="<%= chocolate ? chocolate.brand : '' %>" name="brand" required><br>
        <br>
        <label for="price">Price:</label><br>
        <input id="price" type="number" step="0.01" value="<%= chocolate ? chocolate.price : '' %>" name="price" required><br>
        <br>
        <label for="weight">Weight (grams):</label><br>
        <input id="weight" type="number" value="<%= chocolate ? chocolate.weight : '' %>" name="weight" required><br>
        <br>
        <button type="submit"><%= chocolate ? "Update chocolate" : "Add chocolate" %></button><br><br>
    </form>
    <a href="/api/chocolates">Back to list</a>
</body>
</html>
